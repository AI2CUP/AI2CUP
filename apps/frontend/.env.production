# Default for same-origin deployments (e.g. Docker).
# For Vercel → Render split deployment, set VITE_API_URL in Vercel dashboard
# to the full Render backend URL, e.g. https://ai2cup-backend.onrender.com/api/v1
VITE_API_URL=/api/v1
VITE_APP_TITLE=AI2CUP
