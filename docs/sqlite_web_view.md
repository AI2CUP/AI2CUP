### To view the database directly from browser, you can use the `sqlite_web` tool.

### Run the following commands in the terminal:
1. cd apps/backend
2. source venv/bin/activate
3. pip install sqlite_web
4. sqlite_web ai2cup.db

5. then open the browser and go to the link provided by the tool
